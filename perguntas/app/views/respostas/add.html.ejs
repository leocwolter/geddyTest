<div class="hero-unit">
  <form id="resposta-form" class="form-horizontal" action="/respostas" method="POST">
    <fieldset>
      <legend>Create a new Resposta</legend>
      <% if(params.errors) { %>
      <div class="control-group">
        <ul>
        <% for(var err in params.errors) { %>
          <li><%= params.errors[err]; %></li>
        <% } %>
        </ul>
      </div>
      <% } %>

      <%- partial('form', {resposta: params, perguntas: perguntas}) %>

      <div class="form-actions">
        <%- contentTag('input', 'Add', {type: 'submit', class: 'btn btn-primary'}) %>
      </div>
    </fieldset>
  </form>
</div>
